# secrets never live here, but guard against accidents
*.env
.env
.env.*
!*.env.example

# encrypted vault (ciphertext, but keep it out of git anyway) + passphrase
.vault/

# input + output media
uploads/
out/
web/ad-script.local.json

# python
__pycache__/
*.pyc
.venv/
*.egg-info/
node_modules/

# vendored third-party binaries (Rhubarb ~87MB) — fetched by scripts/setup-avatar-chain.sh
vendor/

# rendered ad output (large, reproducible from scripts/build-ad-au2.sh)
out/ads/
